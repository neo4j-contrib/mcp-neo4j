[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests requiring external dependencies
    unit: marks tests as unit tests with no external dependencies
    mcp_compliance: marks tests that verify MCP protocol compliance
    live: marks tests that require live running services
